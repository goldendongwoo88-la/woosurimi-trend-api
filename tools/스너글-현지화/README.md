# 스너글 쇼츠 현지화 — 다시 돌릴 때 쓰는 것

원래 임시폴더에만 있던 것을 옮겨왔습니다. 기록은 `docs/세션기록-2026-0905-스너글쇼츠-15개국어.md`.

| 파일 | 쓰임 |
|---|---|
| `ja_dub.py` | 일본어 더빙 만들기. `captions.ja.sbv` 의 시간 + `ja_lines.txt` 의 대본을 Kokoro(CPU)로 읽힘 |
| `ja_lines.txt` | 더빙 전용 일본어 대본 19줄 233자 (자막 글자를 그대로 읽히면 칸을 넘침) |
| `verify_ja.py` | 되받아 적기 검수 |
| `썸네일_언어별.py` | 한국어 박힌 띠(y 2320~2620)를 흐리고 번역문을 앉힘. 아랍어는 reshaper+bidi 통과 |

**목소리 복제본으로 갈아끼울 때** — 보이스박스에 「골든 일본어」「차수리미 일본어」 프로필이
`language: ja` 로 등록되면, `ja_dub.py` 의 `jm_kumo`/`jf_alpha` 자리를 그 프로필로 바꿔 돌립니다.
그 뒤 `verify_ja.py` 로 되받아 적기 → 대본과 95% 이상 맞는지 봅니다.

파이썬 경로가 갈립니다 — PIL 은 Python313, torch/kokoro 는 `D:\AI\tts\venv312`.
PATH 의 `python` 은 Python312 라 둘 다 아닙니다. **경로를 통째로 적어서 부릅니다.**
