# 작업 로그 2026-08-30 ~ 08-31 — 복원용 전체 기록

이 세션에서 한 일 전부. 새 세션이 이 파일 하나만 읽으면 이어서 일할 수 있게 적었다.

---

## 1. 환경 복원 (새 컴퓨터)

- 하드웨어 교체 후 9개 golden-* 에이전트 앱 복원 완료.
- Node.js는 PATH에 없음 → **`C:\nodejs-portable\node-v20.15.0-win-x64\node.exe`** 절대경로로 실행.
- 골든 블로그 플로어 서버: 포트 **8485**, `node golden-blog-floor/server.js`.
  - 서버가 네이버 API 429 오류 후 죽는 일 있었음(exit 4) → 재시작하면 됨.
  - 포트 충돌 시: `Get-NetTCPConnection -LocalPort 8485 -State Listen | % { Stop-Process -Id $_.OwningProcess -Force }`

## 2. 네트워크 / 아내 PC

- **사장님 PC**: 유선, 공인 IP 직접 할당(222.238.119.92) — 사설 IP 없음.
- **아내 PC**: WiFi, 192.168.45.173 — **서로 다른 네트워크 대역**이라 로컬 서버 공유 불가.
- 결론: 사장님은 `localhost:8485`, 아내는 배포 서버(`https://woosurimi-trend-api.onrender.com`) 사용.
- 사장님이 WiFi로 옮기면 같은 대역이 되어 IP 공유 가능 (보류 상태).
- 블로그 ID: 사장님 **man_is_best**, 아내 **cha_23**.
- 우수리미 로그인: goldendongwoo88@gmail.com. "접속 코드"라는 것은 따로 없음 — 확장 설정엔 블로그 ID만 넣으면 됨.
- 확장 설정: 개발 모드 ON=localhost:8485, OFF=onrender.com. 둘 다 저장 완료.

## 3. 크롬 확장 — 통합 복사 대공사 (1.27.15 → 1.27.17)

### 문제
발행 큐 "통합 복사" → 네이버 본문 Ctrl+V 하면:
제목이 제목칸에 안 들어가고, [소제목]이 네이버 공식 소제목으로 안 바뀌고, (한때) 강조도 안 실림.

### 실패한 시도들 (다시 하지 말 것)
1. 클립보드 HTML에 표식(data-wsu-post 등) 심고 paste 이벤트에서 읽기 → **paste 이벤트가 확장까지 안 옴** (진단 alert조차 안 떴음).
2. 붙여넣기를 가로채서 대신 넣기 → 위험하고 작동 안 함.
3. 강조를 확장에서 다시 걸기 → **굵게는 토글이라 이미 걸린 걸 또 걸면 풀림.** 강조·형광·색은 클립보드 서식으로 이미 잘 들어감 — 건드리면 안 됨.

### 최종 설계 (1.27.16, 작동 확인 필요)
- **서버 인계함 방식**: 통합 복사 시 `POST /api/last-copied`에 원고 id 기록 (golden-blog-floor/server.js, CORS 열림).
- 확장이 1.5초마다 인계함 확인(`GET /api/last-copied` → `GET /api/posts/:id`) — **반드시 배경(background.js `floorGet`) 경유**. 이유: 네이버는 https, 플로어는 http라 콘텐츠 스크립트 직접 fetch는 mixed content로 차단됨 (실측 "Failed to fetch").
- 본문이 채워지면(=Ctrl+V 완료) 제목 넣기(applyTitle) + 본문 위 중복 제목줄 제거 + 공식 소제목 클릭(applyOfficialSubheads) + 임시저장.
- 안전장치: 다른 글이 열려있으면(원고 소제목이 본문에 하나도 없으면) **아무것도 안 건드림**. 이 창 열기 전 복사본도 무시. 편집기 있는 프레임에서만 실행.
- 붙여넣기로 안 되는 건 **제목칸·공식 소제목 딱 둘** — 네이버가 진짜 클릭을 요구. 나머지 서식은 클립보드가 해결.

### 고친 버그
- `draft-insert.js` applyOfficialSubheads: 문단이 화면 밖이면 클릭 좌표가 빗나가 소제목 0/6 전멸 → **scrollIntoView({block:"center"}) 추가** (라인 ~462).
- visibleByText가 우리 도구 요소를 네이버 버튼으로 착각하지 않게 제외 목록 확장.

### UI 변경 (사장님 지시)
- 하단 가로 도구막대 → **오른쪽 세로**(#ws-tools-dock), 글자수·사진·영상·링크 → **왼쪽 아래**(#ws-tools-counts). panel.css 대개편.
- **제목 띠**(#ws-title-bar) 신설: 스크롤 내려도 네이버 도구줄 바로 아래 가운데에 현재 제목 표시. 0.8초 주기 갱신, pointer-events:none.
- 파란 "우수리미 포스팅" 패널(naver-editor.js): **기본 숨김**. 팝업에서 insertDraft로 부를 때만 뜸.

### 워드 버튼 (1.27.17)
- 사진 안 들어가던 원인: pstatic.net CORS → **배경 fetchImage 경유** + manifest에 `https://*.pstatic.net/*` 권한 추가.
- 서식 안 들어가던 원인: 네이버 서식은 클래스(se-fs-fs19 등)인데 워드가 모름 → **wordInlineStyles()**로 computed style을 인라인으로 새겨 내보냄.
- `<\blockquote>` 오타 수정됨.

### 확장 설치 경로 (중요)
크롬에 로드된 폴더 = `C:\Users\김동우\OneDrive\Desktop\my-project\woosurimi-trend-api\extension` (ID dibknojnbgijeohhfoooiciileelagje).
존재하지 않는 폴더를 가리키는 유령 확장(oblnngon..., OneDrive 없는 경로)이 하나 더 등록돼 있음 → 삭제 권장.
코드 수정 후엔 chrome://extensions 🔄 + **네이버 탭 완전히 닫고 새로 열기** 필수.

## 4. 홈판 벤치마킹 실측 분석 (AI 0원)

두 세션이 독립 측정 → 결론 수렴(교차 검증됨).
- 이 세션: 33곳 131편 (RSS → m.blog PostView 파싱). 스크립트: 스크래치패드 `deep-analyze.mjs`, `report.mjs`, 데이터 `top.json`/`mine.json`.
- 동료 세션: 본문 130편/25곳, 제목 1,100개/55곳, 썸네일 280장. 문서: **`docs/홈판-벤치마킹-실측-2026-08-31.md`** ← 정본.

### 핵심 격차 (사장님 vs 벤치 중앙값)
1. **글자색 1,000자당 53.5회 vs 0~3.5회 (15배 과다)** — 벤치 89%는 글자색 아예 안 씀. 형광 92%·밑줄 93% 미사용. 쓰는 강조는 사실상 굵게 하나.
2. 문단 41자(45자 초과 43%) vs 16~24자(0~7%) → 20자 안팎으로.
3. 사진 간격 148자 vs 68~101자 → 80~100자마다 한 장.
4. 도입부 101자·질문 0% vs 48~72자·질문 10~28% → 50~70자, 질문/장면으로 열기.
5. Q&A 있는 글 20% vs 28~48% → 글 중간 짧은 질문 2~3개.

### 제목
- 따옴표 65%로 이미 과다(벤치 9~29%) — 늘리지 말 것.
- 늘릴 것: 앞머리 끊기(..) 15%→35%+, 주어 감추기 20%→35%+, 반전 접속(~인데..) 0%→사용.
- 실명은 줄이기. 패션 공식: `[놀랄 조건].. + [반전] + [나이]세 [여배우/연예인] + [품목]`
- 기각된 가설: "전언(~다더니) 격차" — 표본 1,100개로 재니 격차 없음(첫 131편 표본의 착시).

### 유지할 것 (이미 앞섬)
동영상 50%(벤치 3~13%), 다음글 링크 100%(벤치 38~63%), 숫자 박기 50%.

### 반영된 곳 (숫자 정본: woosurimi-trend-api/src/homefeedRules.js 의 MATE/THUMB)
- `src/promptStudio.js` mateBlock → 스킬 4종(새벽뷰티·새벽패션·연예뷰티·연예패션)에 자동 주입.
- `golden-blog-floor/src/homefeed.js` bodyShapeBlock(MATE_2608) → 원고 생성 지침.
- **이 세션이 고친 모순 2곳**:
  - `golden-blog-floor/src/prompts.js` FORMAT_MARKERS: "형광·색·크게 필수" → 실측 상한제로 뒤집음(굵게 주력 1,000자당 3~8, 색 3~4, 형광 최대 1, 밑줄·크게 금지).
  - `golden-blog-floor/src/reviewRules.js`: "형광 없음(필수)"이라며 자동보완 루프가 색을 도로 채우던 검사 → 부재는 칭찬, 과다만 지적(길이 비례 상한). 굵게 상한도 1,000자당 8로 길이 비례화.
- 동료 세션이 draft-insert.js에 **45자 자동 줄바꿈**(splitLongText) 추가함.

### 산출물
- 보고서 아티팩트: https://claude.ai/code/artifact/a4436e08-8723-41bd-8bc8-5490a698fe13
- PDF: 스크래치패드 `홈판-상위노출-격차-진단.pdf` (사장님께 전송 완료). PDF 재생성법: report-print.html을 크롬 headless `--print-to-pdf`로 (user-data-dir 지정 필요).

## 5. git 상태

- 브랜치 **feat/homefeed-mate-2608** (main에서 분기), 커밋 914aa74에 세 세션 작업 4,504줄.
- 1.27.17 워드 수정분은 그 커밋 이후 — 아직 미커밋.
- 확장 1.27.17 실사용 검증 후 main 합류 예정.
- ⚠️ **golden-blog-floor는 git 저장소가 아님** — prompts.js/reviewRules.js/homefeed.js/server.js/app.js 수정분 버전관리 안 됨.

## 6. 스킬 설치

- `i-have-adhd` — 전역(`~\.agents\skills\`). /명령어가 아니라 상시 행동규칙.
- `frontend-design`(anthropics/skills) — 전역 + git clone으로 `woosurimi-trend-api/.claude/skills/frontend-design/`에도 설치.
- 규칙(메모리 저장됨): **스킬 설치는 항상 `--global -y`**.

## 7. 미해결 / 다음 할 일

1. **통합 복사 1.27.16~17 실사용 검증** — 확장 새로고침 후 제목·소제목 자동 처리가 실제로 되는지. "통합 복사 마무리 — 제목 넣음 · 소제목 n/n" 패널이 떠야 함.
2. 워드 버튼 검증 — 사진 임베드 + 서식 유지 확인 (pstatic 권한 승인 필요할 수 있음).
3. 검증 후 feat/homefeed-mate-2608 → main 병합.
4. golden-blog-floor git init 검토 (버전관리 공백).
5. 유령 확장(비-OneDrive 경로) 삭제.
6. 아내 PC — 배포 서버 로그인/확장 설정 마무리 (블로그 ID cha_23, 개발모드 OFF).
7. 새 원고부터 실측 규칙(글자색 축소 등)이 실제 반영되는지 발행 1건으로 확인.

## 8. 자주 쓰는 명령

```bash
# 서버 시작 (git bash)
cd /c/Users/김동우/OneDrive/Desktop/my-project/golden-blog-floor && export PATH="/c/nodejs-portable/node-v20.15.0-win-x64:$PATH" && node server.js

# 문법 검사
/c/nodejs-portable/node-v20.15.0-win-x64/node.exe --check <파일>

# 인계함 확인
curl -s http://localhost:8485/api/last-copied
```

---

## 같은 날 다른 세션 기록 (2026-08-31 추가)

세션 세 곳이 동시에 일했습니다. 이 문서는 **환경·네트워크·확장** 쪽입니다.
다른 쪽은 아래에 있습니다 — 복원할 때 같이 보세요.

- `docs/작업기록-2026-08-31.md` — 벤치마킹 실측 · 원고 지침 반영 · 썸네일 · 자동 줄바꿈
- `docs/홈판-벤치마킹-실측-2026-08-31.md` — 실측 결과 본문(표·제목 공식·교차검증)
- `docs/세션로그-2026-08-31-수익화-착수.md` — 수익화 라인
- `docs/session-log-video-pipeline.md` — 사주릴스 영상

⚠️ 코드는 브랜치 `feat/homefeed-mate-2608`의 커밋 `914aa74`에 담겨 있습니다(main 아님).
