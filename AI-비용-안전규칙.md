# AI 비용 안전규칙 — 모든 에이전트·프로그램 공통

> 2026-08-28 사장님 지시. 배경: 사주 앱이 서버 재시작마다 fable-5로 해석 28건을
> 재생성(장부도 없이)해 **$20이 하루 만에 증발**한 사건. 같은 일이 구조적으로
> 불가능하도록, 앞으로 만드는 모든 것에 아래 8조를 적용한다.
> (이 문서는 my-project의 모든 세션이 공유한다. 새 에이전트를 만들 때 이 문서를 먼저 읽을 것.)

## 제1조 — 장부 없이 AI 호출 금지
AI를 부르는 앱은 **첫 호출 전에 지출 장부부터** 만든다 (일자·기능·모델·토큰 기록).
장부 없는 AI 호출이 이번 사건의 "조용히 타는" 원인이었다.

## 제2조 — 자동 실행에는 일일 상한 필수
사람이 버튼을 누른 게 아닌 모든 자동 호출(워밍·크론·배치·자동 재시도)에는
**일일 예산 게이트**를 건다. 초과하면 스스로 멈추고 로그에 남긴다.
사람이 누른 유료 기능(손님 결제 리포트 등)은 예산과 분리한다 — 매출 길은 안 막는다.

## 제3조 — 서버 시작 시 AI 호출은 원칙 금지
시작할 때 미리 만들기가 꼭 필요하면 세 조건을 전부 갖춘다:
① 디스크 캐시를 먼저 확인(있으면 재생성 금지) ② 제2조의 예산 게이트
③ 배포 환경에서만 실행(로컬·시험 기동은 환경변수로 자동 차단).
"재시작 = 전부 재생성" 구조는 금지.

## 제4조 — 모델은 두 단, 기본은 절약
- **비싼 모델(fable-5·opus)**: 손님이 돈 낸 결과물 전용.
- **절약 모델(sonnet·haiku)**: 내부용·유입용·가족용·검토용 전부. **기본값은 절약 모델.**
비싼 모델을 쓰려면 "이건 돈 받는 물건"이라는 근거가 코드 주석에 있어야 한다.

## 제5조 — 같은 걸 두 번 만들지 않는다
생성 결과는 디스크에 캐시한다(메모리만 금지). 내용이 안 바뀌는 것(평생사주류)은
영구 캐시, 날짜 것은 날짜 키로.

## 제6조 — 시험은 0원
테스트·시험 기동·개발 중 실행은 AI를 부르지 않는다(우회 경로 또는 가짜 응답).
.env에 진짜 키가 있어도 시험이 돈을 태우면 안 된다.

## 제7조 — 신설 기능은 견적 먼저
새 AI 기능은 ①단가 1회 실측 → ②하루 예상 호출 수 × 단가를 사장님께 보고 →
③승인 후 상시 가동. 단가 미실측 모델(fable-5)은 원화 환산을 지어내지 말고
토큰으로 말한다.

## 제8조 — 호출 자체를 아끼는 습관
- maxTokens는 필요한 만큼만 (넉넉히 ≠ 안전).
- 자동 재시도는 최대 1회.
- 규칙·계산으로 되는 건 AI를 부르지 않는다 (분류·집계·순위·D-day 등 — 0원 도구 원칙).
- 같은 시스템 프롬프트 연속 호출은 붙여서(캐시 적중) 돌린다.

---
적용 현황: 사주 앱(8조 전부, 2026-08-28 배포) · 우수리미(장부·절약형 기본·캐시) ·
골든 에이전트 8484·8485(1·2·4조 기존 + 3·6조 점검 완료, 2026-08-28: 부팅 시 자동 시작되는
루프—포지션 모니터·가격 알림 체커—전부 committee 룰/시세 모듈만 import, AI 호출 无 확인.
cost_log 항상 요청 핸들러 안에서만 add — 서버만 켜면 지출 0원. 통과).
