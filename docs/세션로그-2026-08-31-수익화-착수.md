# 세션 로그 — 2026-08-31 · 수익화 착수

> **이 문서의 목적**: 세션이 끊기거나 나중에 다른 세션이 이어받을 때 여기만 읽으면 복원되도록 남기는 기록입니다.
> 새 세션에서 이어가려면: 이 파일 + `골든-에이전트-모음.md` + `수익화-프로세스-맵.md` 3개를 읽으면 됩니다.

---

## 0. 대화 흐름 (사장님 지시 순서)

| # | 사장님 지시 | 세션 대응 |
|---|---|---|
| 1 | "이 에이전트에는 어떤일을 하고 있는거야?" | 저장소 전수 조사 → 아래 §1 "저장소 현황" 보고 |
| 2 | "전자책 수익화 할 수있게 지금 진행해줘" | 전자책 라인 실사 착수 |
| 3 | "전자책 뿐만 아니라 지금 당장 수익화 할 수 있는 방법 우선순위대로 알려주고 순서대로 진행해서 돈벌어와" | 우선순위 확정(§3) 후 1번부터 제작 착수 |
| 4 | "지금까지 대화나눈거 전부다 기록 남겨줘 나중에 복원할 수 있게" | 이 문서 작성 |

**비용 원칙 준수**: 이 세션의 모든 작업은 **결정론 코드 = 0원**. AI 실호출은 하지 않았습니다
(CLAUDE.md 「MAX 20X 우선」 정책 · `AI-비용-안전규칙.md`).

---

## 1. 저장소 현황 (2026-08-31 실사)

### `woosurimi-trend-api` — 이름은 트렌드 API지만 실체는 "블로그 편집국 본체 + 크롬 확장"

- **백엔드**: Express + node-cron. 원래 구글 트렌드 KR 수집(`/api/trends`)이 목적이었으나
  현재 `src/` 아래 **80여 개 모듈**로 성장 — 블로그 원고·제목·맞춤법·법률검토·썸네일,
  네이버 키워드/순위/플레이스, 숏폼 렌더(ffmpeg)·자막·BGM, 스레드/인스타 발행.
- **크롬 확장** `extension/` "우수리미 포스팅" — 네이버 블로그 에디터 자동화. 발행은 사장님이 직접.
- **배포**: https://woosurimi-trend-api.onrender.com (Render 무료 플랜)
- **역할**: 9개 에이전트 생태계 중 **② 블로그 AI 편집국(8485)** 담당

### 미커밋 변경분 (이 세션 시작 시점에 이미 있던 것 — 손대지 않음)

| 파일 | 내용 |
|---|---|
| `extension/content/editor-tools.js` (+112줄) | 골든 블로그 플로어 [통합 복사] → 네이버 Ctrl+V **자동 마무리**. 클립보드 표식 방식이 실패(붙여넣기 이벤트가 확장까지 안 옴)해서, 서버 `/api/last-copied` 인계함을 1.5초마다 폴링 → 본문이 채워지면 제목·소제목·강조 자동 적용 방식으로 전환 |
| `background.js`·`options.*`·`manifest.json` | **개발 모드 토글** (ON이면 `localhost:8485` 사용). 확장 v1.27.15 → **1.27.16** |
| `src/videoRenderer.js` | 켄번즈 팬을 가로 1방향 → **대각선 4방향 순환**으로 개선 |

---

## 2. 수익 라인 실사 결과 (⚠ 문서와 실제가 다른 부분 포함)

`밤샘작업-로그-0828.md`에 "완료"로 적힌 산출물 중 **실제로는 없는 것**이 있었습니다.

| 항목 | 문서상 | 실제 확인 결과 |
|---|---|---|
| 전자책 원고 | 북 팩토리에서 집필 | ⚠ `golden-book-factory/data/books/` **비어 있음**. 대신 **`woosurimi-trend-api/` 루트에 완성 원고 2종이 있음**(아래) |
| 디지털 템플릿 5종 PDF | 바탕화면 `골든-디지털템플릿-5종\` 저장 | ⚠ 해당 폴더 **없음**. 생성 코드(`template.js`)는 정상 → 재생성하면 됨 |
| 상세페이지 팩토리 | 7부서 완비 | 코드는 있음. `data/` 비어 있음(제작물 0건) |

### ✅ 발견 — 완성된 전자책 원고 2종 (가장 큰 자산)

| 파일 | 제목 | 분량 |
|---|---|---|
| `ebook-draft.json` | **2026년 네이버 블로그 상위노출** — 연관검색어 사라진 시대, 바뀐 로직에 맞춰 다시 쓰는 법 | 서문 1,464자 + **8장 18,583자** = 약 20,047자 |
| `ebook2-draft.json` | **AI 부업 영상 250개를 분석해봤습니다** — 실제 숫자로 본 현실 | 서문 1,189자 + **7장 18,015자** = 약 19,204자 |

**데이터 구조** (스크립트 작성 시 참고):
```
{ book: { title, subtitle, promise, chapters:[{title, points:[]}] },
  parts: { intro: "마크다운 문자열", chapters: ["# 1장. ...\n\n본문...", ...] } }
```
- `parts.chapters`는 **각 장의 전체 본문 문자열 배열** (제목 `# N장. ...`이 첫 줄에 포함됨)
- `book.chapters[].points`는 목차용 소주제. **`body` 필드는 없음** → 북 팩토리 포맷
  (`chapters:[{title, body}]`)으로 **변환이 필요**함

### 북 팩토리(8487)에서 0원으로 쓸 수 있는 것 — 전부 결정론

| 기능 | 위치 | 비고 |
|---|---|---|
| EPUB 생성 | `src/epub.js` `buildEpub()` | 의존성 없는 자체 ZIP 구현 |
| 인쇄용 HTML(→PDF) | `src/epub.js` `buildPrintHtml()` | Ctrl+P로 PDF 저장 |
| 맛보기 EPUB | `server.js` `/api/export/sample` | 서문+1챕터+전체목차 |
| 판매 키트(무키) | `src/saleskit.js` `templateKit()` | 상세페이지 문안·FAQ·키워드 |
| 홍보 키트(무키) | `src/saleskit.js` `templatePromo()` | 스레드 3종·블로그 서두·인스타 |
| 가격 제안 | `src/saleskit.js` `priceSuggestion()` | 글자수 기반 룰. **2만자 → 9,900원(런칭) / 14,900원(정가)** |
| 디지털 템플릿 5종 | `src/template.js` | 습관·주간·가계부·콘텐츠·독서 |
| 판매 장부 | `src/revenue.js` | 등록상태·판매건수·실입금 |

> `makeSalesKit`/`makePromoKit`는 AI 호출판. **키 없으면 자동으로 템플릿판으로 폴백** → 0원 유지.

---

## 3. 확정된 수익화 우선순위 (2026-08-31)

기준: **첫 입금까지 걸리는 시간 × 마진 × 사장님 손이 필요한 양**

| 순위 | 라인 | 지금 상태 | 첫 입금까지 | 원가 | 사장님이 해야 할 일 |
|---|---|---|---|---|---|
| **1** | **전자책 2종** | 원고 완성(2만자×2) | 등록 1~3일 + 홍보 1~2주 | 0원 | 크몽/유페이퍼 계정 + 등록 클릭 |
| **2** | **디지털 템플릿 5종** | 코드 완비, 재생성 필요 | 등록 즉시~3일 | 0원 | 크몽 등록 (전자책과 같은 계정) |
| **3** | **상세페이지 대행** | 코드 완비, 샘플 0건 | 샘플 제작 후 1~2주 | 0~30원 | 크몽 서비스 등록 + 의뢰 응대 |
| 4 | 블로그(애드포스트·체험단) | 운영 중 | 이미 진행 중 | 0원 | 기존 루틴 유지 |
| 5 | 유튜브·쇼츠(YPP) | 채널 축적 필요 | 수개월 | 낮음 | 매일 업로드 |

**핵심 병목은 딱 하나 — 크몽 계정과 등록.** 그건 제가 할 수 없습니다
(계정 생성·발행은 사장님 본인 확인이 필요한 영역). 나머지 "팔 수 있는 완제품"은 제가 전부 만듭니다.

---

## 4. 진행 상황

- [x] 저장소·수익 라인 전수 실사
- [x] 우선순위 확정
- [x] 이 로그 문서 작성
- [ ] **1순위 — 전자책 2종 완제품화** (EPUB·인쇄PDF·맛보기·상세페이지 문안·가격표) ← 진행 중
- [ ] 2순위 — 디지털 템플릿 5종 PDF 재생성
- [ ] 3순위 — 상세페이지 샘플 포트폴리오 3종
- [ ] 사장님 인계 — 크몽 등록 절차서

---

## 5. 복원용 메모 (환경)

- **node가 PATH에 없음**: `export PATH="/c/nodejs-portable/node-v20.15.0-win-x64:$PATH"`
- 프로젝트 밖에서 실행 시 `NODE_PATH`를 프로젝트 `node_modules`로 지정
- curl로 한글 본문 전송 시 깨짐 → JSON을 utf8 파일로 저장해 `--data-binary @파일`
- 포트: 8484 증권 / **8485 블로그** / 8486 스냅 / **8487 북** / 8488 이코노미 / 8489 쇼츠 / 8490 스토리 / **8491 상세페이지** / 8492 커플튜브
