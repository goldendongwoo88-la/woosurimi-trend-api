# 이 저장소에서 작업하는 규칙

우수리미부부 콘텐츠 자동화 — 트렌드 API + 숏폼 엔진(골든컷).

아래 규칙은 **실제로 사고가 났던 것들만** 적었습니다. 일반론은 없습니다.
지키지 않으면 사장님이 같은 말을 두 번 하시게 됩니다.

---

## 1. 영상 길이는 영상 스트림으로 잰다 (훅으로 막혀 있음)

```
ffprobe -v error -select_streams v:0 -show_entries stream=duration,nb_frames -of csv=p=0 <파일>
```

**컨테이너 길이(`-show_entries format=duration`)를 쓰지 않습니다.** 이 값은 영상과 소리 중
**긴 쪽**을 돌려줍니다. 장면 소리는 `apad`로 목표 길이까지 채워지므로, 그림이 잘려도 이
값은 멀쩡해 보입니다.

> 2026-09-18에 실제로 **그림 2.17초 / 소리 13.99초**인 파일이 "13.99초 정상"으로 보고됐습니다.
> 사진 입력의 `-loop 1`에 `-framerate`가 빠져 25fps로 디코딩되던 것이 원인이었고,
> 컨테이너 길이만 보다가 한참 못 잡았습니다.

`.claude/hooks/video-guard.sh`가 이걸 **기계적으로 차단**합니다. 소리 길이를 일부러 잴 때는
`-select_streams a:0`을 붙이면 통과합니다.

같은 훅이 `ffmpeg -loop 1`에 `-framerate`가 빠진 것도 차단합니다.

## 2. 확인 전에는 "정상"이라고 쓰지 않는다

- 렌더 결과를 보고할 때는 **프레임 수까지** 확인하고 적습니다. (15초 / 30fps → 450프레임 근처)
- 다른 세션이나 문서의 주장은 **"그쪽 보고"라고 표시**하고, 내가 확인한 것처럼 옮겨 적지 않습니다.
- 파일을 못 봤으면 "못 봤다"를 **먼저** 말합니다. 추측으로 품질을 판단하지 않습니다.

## 3. 프로젝트 위치 — 묻기 전에 여기부터 본다

| 프로젝트 | 위치 | 클라우드 세션에서 |
|---|---|---|
| **woosurimi-trend-api** (이 저장소) | GitHub | 만질 수 있음 |
| **couple-studio** (작업실 · 18762) | `C:\Users\Admin\Documents\Codex\2026-09-18\new-chat\couple-studio` — git 아님 | **접근 불가** |
| **GoldenCut** (18741) | `C:\Users\Admin\Documents\Codex\2026-09-17\new-chat-6\outputs\GoldenCut` | **접근 불가** |
| 새 작업 공간 규칙 | `C:\Users\Admin\ai-workspace` (다른 세션이 이전 예정) | 참고만 |

GitHub에 없는 로컬 폴더는 **이 표를 먼저 보고**, 그래도 필요하면 그때 여쭙습니다.
사장님 PC에 있는 파일(`autocut.py`, `linkvideo.py`, `voice_pick.py` 등)은 클라우드 세션에서
열 수 없습니다. 보려면 GitHub에 올리거나 내용을 붙여넣어 주셔야 합니다.

## 4. 클라우드 세션의 한계 — 먼저 말하고 시작한다

- 사장님 PC 파일을 못 봅니다
- 다른 Claude 세션에 메시지를 못 보냅니다 (권한 없음)
- 사장님 PC의 로컬 포트(18741, 18762)에 못 붙습니다

막히면 **"권한이 없다"를 먼저 말하고 대안을 같이 냅니다.** 돌려 말하지 않습니다.

## 5. 세션 분담 — 같은 걸 두 번 만들지 않는다

작업실(couple-studio)은 그 세션이, 이 저장소는 여기가 맡습니다.
한 프로젝트를 두 세션이 만지면 충돌합니다 (실제로 포트 18762와 `server.py` 인증에서 부딪혔습니다).

**같은 기능을 두 곳에 만들기 전에 사장님께 먼저 확인합니다.**
공용 규격은 `goldencut/styles.json`처럼 **언어 중립 파일로 한 벌만** 둡니다 —
자바스크립트와 파이썬이 같은 값을 따로 들고 있으면 반드시 어긋납니다.

## 6. 골든컷 엔진의 지뢰

`goldencut/goldencut_effects.py` 안에 ⚠️ 주석으로 이유까지 적어뒀습니다.
요약만 적으면 이유를 잃어버리니, **고치기 전에 그 주석을 읽으세요.**

- 자막은 `.srt`가 아니라 `.ass` — `.srt`에 색 태그를 넣으면 ffmpeg가 먹어버립니다
- `-shortest` 금지 — 나레이션이 장면보다 짧으면 장면이 잘려 전환 offset이 어긋납니다
- 영상+사진을 섞으면 `settb/asettb`로 타임베이스를 맞춰야 합니다
- 출력은 1080×1920 / 30fps

더 자세한 건 [docs/작업실-통합-계획.md](docs/작업실-통합-계획.md)의 「알려진 함정」을 보세요.

---

## 열린 결정 — 사장님 답을 기다리는 것

다음 세션은 이걸 **다시 여쭙지 말고** 이어받으세요. 답이 오면 이 항목을 지웁니다.

1. **작업실 세션에 전달** — `-select_streams v:0` 함정을 couple-studio 쪽에 알려야 합니다.
   그쪽 테스트 41개가 컨테이너 길이로만 확인했다면 같은 결함이 그대로 통과합니다.
   클라우드 세션은 세션 간 메시지 권한이 없어 사장님이 전해주셔야 합니다.

2. **링크→영상 중복 정리** — 작업실 `linkvideo.py`(링크 1개 → 영상 1개)와
   이 저장소 `goldencut/integration/autocut.js`(링크 1개 → 추천 6개)가 같은 일을 두 언어로 합니다.
   어느 쪽을 본체로 삼을지 정해지면 나머지를 거기에 맞춥니다.

---

## 구조 한 눈에

```
src/index.js              서버 (포트 하나, PORT || 3000). 화면은 전부 public/ 아래
  └ saasRoutes.js         회원·요금제·진단·순위추적
  └ shortformPlanner.js   링크 → 장면 기획 (블로그·쇼핑·여행·뉴스)
  └ videoRenderer.js      서버판 렌더 (720×1280, Render 메모리 한계)

goldencut/                로컬판 숏폼 엔진 (1080×1920). 작업실이 복사해서 씁니다
  ├ goldencut_effects.py  화면효과 6종 + 렌더
  ├ goldencut_caption.py  자동 캡션
  ├ goldencut_voice.py    더빙 (Voicebox)
  ├ styles.json           스타일 프리셋 10종 — JS/파이썬 공용
  └ integration/          작업실에 붙이는 연결 코드 + 설명서
```

> ⚠️ 8489·8486을 따로 켤 필요 없습니다. 쇼츠·카드뉴스·유튜브파인더는 **같은 서버의 다른 화면**이라
> `npm start` 한 번이면 전부 열립니다.
