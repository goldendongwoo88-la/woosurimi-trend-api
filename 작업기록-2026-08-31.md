# 작업 기록 — 2026-08-31

> 나중에 이 세션 내용을 되살리기 위한 기록입니다. "무엇을 왜 그렇게 했는지"까지 적습니다.
> 숫자와 결론만 남기면 다음에 같은 판단을 다시 못 합니다.

---

## 0. 한눈에

| 갈래 | 내용 | 상태 |
|---|---|---|
| 기능 | 발행됨 처리 → 다음날 원고 자동 삭제 | 완료·실동작 검증 |
| 지침 | 뷰티 원고 4가지 규칙(화장품 검색·키워드 분리·헤어 확대·제목 혼합) | 완료 |
| 분석 | 홈판 상위노출 실측 72편 → 리포트·PDF | 완료 |
| 지침 | 분석 결과를 스킬 전체에 반영 | 완료 |
| 설치 | 스킬 5종(caveman·superpowers·stop-slop·ui-ux·obsidian) 전역 | 완료 |
| 인프라 | golden-blog-floor 최초 git 저장소화 | 완료 (`cec56ad`) |
| 정책 | MAX 20X 우선 + "PC 켜져있을 때만" 원칙 | 메모리 기록 |
| 미완 | 옵시디언 vault 연결 | **막힘 — 옵시디언 미설치** |

---

## 1. golden-blog-floor — 기능 변경

### 1-1. 발행됨 처리하면 다음날 원고가 사라진다

**요청**: "발행됨 처리 이부분은 내가 누를 수 있게 해주고, 내가 누르면 다음날 이 글은 삭제되게. 원고 보관함에서도 삭제."

**구현**

| 파일 | 무엇 |
|---|---|
| `server.js` `/api/posts/:id/pub` | 발행됨으로 바뀌는 순간 `deleteAt = 지금 + 24시간` 기록 |
| `server.js` `/api/queue` | 큐를 열 때마다 만료분 청소 |
| `server.js` 기동부 | 1시간마다 청소 (`setInterval`) |
| `src/posts.js` | `sweepExpiredPublished()` — `발행됨` + `deleteAt` 지남 → 파일 삭제 |

**검증 방법(그대로 다시 할 수 있음)**: 더미 원고를 `data/posts/`에 만들고 → `/pub`으로 발행됨 처리 → `deleteAt`이 +24시간으로 찍히는지 확인 → `deleteAt`을 과거로 바꾼 뒤 `/api/queue` 호출 → 파일이 실제로 사라지는지 확인. 사장님 실제 글은 건드리지 않았음.

**왜 서버가 켜져 있을 때만 도는가**: `setInterval`은 프로세스가 살아 있는 동안만 돕니다. 사장님 지시("PC 켜져있을 때만 작업")에 맞춘 구조입니다. 클라우드 크론을 쓰지 않았습니다.

> ⚠️ 이후 다른 세션이 여기에 보강을 넣었습니다: 이 기능이 생기기 **전에** 발행됨 처리된 옛 글에는
> `deleteAt`이 없어 영원히 안 지워지는 문제가 있었고, 발견 시점부터 하루를 세어 예약하도록 고쳤습니다.
> 이 보강까지 커밋 `cec56ad`에 포함돼 있습니다.

### 1-2. 뷰티 원고 지침 4가지

**요청**: 화장품 비워두지 말고 검색해서 채우되 못 찾으면 그 내용 자체를 빼기 / 제목에 화장품 키워드와 헤어 키워드 혼용 금지 / 헤어 콘텐츠 비중 확대 / 궁금증 자극 제목에 이름 공개·비공개 섞기.

| 파일 | 무엇 |
|---|---|
| `src/aiwriter.js` | 뷰티는 헤드라인이 없어도 `web_search`를 켠다. 화장품은 못 찾으면 `✏️` 대신 **언급 통째로 삭제** |
| `src/prompts.js` `TREND_ANGLE.beauty` | 헤어 소재는 제품 정보 없어도 채택. 배치마다 최소 1~2개 |
| `src/prompts.js` `TITLE_DEVICES.beauty` | 화장품·헤어 키워드 혼용 금지. 헤어 전용 후킹 카테고리 추가 |
| `src/prompts.js` `DRAFT_BODY.beauty` | 제품형/헤어형 흐름을 분리 |

**왜 `✏️`가 아니라 삭제인가**: 기존 규칙은 못 찾은 사실에 `✏️(확인 필요)`를 남기게 돼 있었습니다.
그러면 원고에 빈칸 표시가 남아 사장님이 손으로 채워야 합니다. 화장품에 한해 그 규칙을 뒤집었고,
날짜·수치 같은 다른 사실은 기존대로 `✏️`를 유지합니다.

### 1-3. 새벽뷰티 스킬은 파일을 못 고칩니다

`saebyeok-beauty` 등은 Anthropic 제공 빌트인 스킬이라 로컬에 편집 가능한 `SKILL.md`가 없습니다
(`~/.claude/skills`, 플러그인 마켓플레이스, CLI 설치 경로 전부 확인함).
그래서 같은 4가지 규칙을 **메모리**에 저장해 세션이 스킬 위에 덧붙이도록 했습니다
→ `memory/feedback_saebyeok_beauty_overrides.md`

---

## 2. 홈판 상위노출 실측 분석

### 무엇을 어떻게 쟀나

- 경쟁 7곳 56편(각 최근 8편) + 사장님 2곳(`runrun0803`, `man_is_best`) 16편 = **72편, 실패 0건**
- 공개 글만. 남의 본문·사진은 세고 버림. **AI 호출 0회 = 0원**
- 스크립트: `scripts/homefeed-deep-v2.js` (수집) / `scripts/homefeed-deep-report2.js` (집계)
- 원자료: `data/homefeed-deep-v2.json`

### 핵심 결론

**글자수는 문제가 아니다. 같은 분량을 어떻게 썰었느냐가 문제다.**

| 항목 | 경쟁(중앙) | 사장님(중앙) |
|---|---|---|
| 문단 평균 | 16자 | **48자** |
| 가장 긴 문단 | 32자 | **143자** |
| 60자 초과 문단 비율 | 0% | **33%** |
| 굵게 | 4회 | **27회** |
| 따옴표로 시작 | 2% | **63%** (과다) |
| 말줄임 `..` | 55% | **6%** (부족) |
| 표 / 영상 / 링크카드 | 9% / 13% / 2개 | 50% / 75% / 3개 (**우리가 앞섬**) |

### 이번에 새로 잡은 것

**`runrun0803`도 같은 문제.** 기존 분석은 `man_is_best`만 봤는데, 지금 메인인 `runrun0803`도
문단 45자·굵게 30회입니다. "옛 블로그 습관"으로 넘길 수 없습니다.

### 틀릴 뻔한 것 — 반드시 남겨둡니다

경쟁 제목 몇 개를 보고 **"제목 뒤에 검색 키워드를 붙이는 게 비결"**이라고 규칙을 만들려 했습니다.
세어보니 **경쟁 34% / 사장님 38%로 우리가 오히려 높았습니다.** 격차가 아니었습니다.

인상만으로 규칙을 만들면 없는 문제를 고치게 됩니다. 그래서 이 사실을 지침에 "격차 아님"으로 못 박았습니다.
대신 블로그별 전략 차이(`sunnyya314` 100% ↔ 홈판 전용 `nidle_831` 0%)를 "고칠 것이 아니라 고를 것"으로 정리.

### 측정의 한계 (먼저 적어둡니다)

1. **상관관계지 인과가 아님.** 네이버는 홈판 기준을 공개하지 않음. "잘 되는 블로그가 이렇게 쓴다"까지만.
2. **조회수 못 잼.** 네이버가 남의 글 조회수를 안 줌.
3. **태그 수 못 잼.** 모바일 글 페이지에 태그가 안 실림 → 0으로 세면 거짓이라 "측정 불가"로 둠.
4. **Q&A는 두 측정이 엇갈림.** 형식 블록(`Q. A.`)은 경쟁 2%인데, **본문 중간 짧은 질문**은 경쟁 28~48%.
   → 경쟁이 쓰는 건 *Q&A라는 형식*이 아니라 *질문이라는 리듬*이었음. 둘 다 그대로 실었음.
5. **표본은 최근 글 편향.** 블로그당 최근 8편이라 계절·이벤트 영향을 받음.

### 산출물

- 리포트(Artifact): https://claude.ai/code/artifact/ae5db699-6d63-4e8c-9df4-bcce0017f54e
- PDF: `바탕화면/홈판-상위노출-해부.pdf` (11쪽)
  - 재생성: `scratchpad/homefeed-report-print.html` → headless Chrome `--print-to-pdf`

### 다른 세션과의 교차검증

다른 세션이 더 큰 표본(본문 130편·제목 1,100개·썸네일 280장)으로 같은 분석을 돌려두었고,
**문단·강조·따옴표 항목에서 결론이 일치**했습니다. 표본과 측정 방법이 다른데 같은 답이 나왔다는 점이
이 숫자들의 신뢰 근거입니다. 그쪽 문서: `docs/홈판-벤치마킹-실측-2026-08-31.md`

---

## 3. 분석 결과를 스킬에 반영

**구조**: 스킬 파일 15개(`src/promptSkills/*.txt`)를 각각 고치지 않고
`src/homefeedRules.js` 한 곳에서 덧대는 방식(`promptStudio.js`의 `homefeedPatch`).
숫자를 한 곳에만 두려는 설계라 그대로 따랐습니다.

**이미 반영돼 있던 것**(다른 세션 작업): 문단 20자, 글자색, 사진 간격, 도입부, 짧은 질문, 제목 끊기·감추기, 썸네일.

**이번에 추가한 것** — `src/homefeedRules.js`

| 키 | 내용 |
|---|---|
| `MATE.verify` | 별도 표본 교차검증 결과 + 두 블로그 모두 같은 문제라는 사실 |
| `MATE.tailKeyword` | 뒷꼬리 품목은 격차가 아니며 전략 선택이라는 기록 |
| `mateBlock()` | 위 둘을 렌더링해 모든 스킬에 전달 |

**검증**: 스킬 10종(`celeb-beauty`=새벽뷰티, `celeb-fashion`=새벽패션 포함) 전부에 두 항목이 들어가는 것 확인.
실제 스킬 프롬프트 조립 결과도 확인(3,512자 → 8,530자).

**앱 쪽**: `golden-blog-floor/src/homefeed.js`의 `bodyShapeBlock`에도 `runrun0803` 사실 추가.
뷰티 원고 지침에 반영되는 것 확인.

---

## 4. 설치한 스킬 (전부 전역 `~/.claude/skills/`)

| 스킬 | 출처 | 비고 |
|---|---|---|
| caveman | `juliusbrussee/caveman` | `~/.claude/commands/caveman.md` 슬래시 명령도 생성 |
| superpowers 14종 | `obra/superpowers` | brainstorming·TDD·systematic-debugging 등 |
| stop-slop | `hardikpandya/stop-slop` | SKILL.md + references 3개 |
| ui-ux-pro-max 7종 | `nextlevelbuilder/ui-ux-pro-max-skill` | design·brand·slides·ui-styling 등 |
| obsidian-second-brain | `eugeniughelbur/obsidian-second-brain` | **vault 미연결** (아래 참고) |

### superpowers SessionStart 훅

원본 훅은 `CLAUDE_PLUGIN_ROOT`가 있어야 Claude Code 형식으로 출력합니다.
플러그인이 아니라 스킬로 깔면 그 변수가 없어 **조용히 아무 일도 안 합니다.**
그래서 원본을 건드리지 않고 래퍼를 하나 끼웠습니다(업데이트해도 안 깨지도록).

- 실체: `~/.claude/superpowers/` (hooks + skills)
- 래퍼: `~/.claude/superpowers/hooks/session-start-cc`
- 등록: `~/.claude/settings.json`의 `hooks.SessionStart`
- 검증: 설정된 그 명령 그대로 실행 → `hookSpecificOutput.additionalContext` 형식 확인

---

## 5. golden-blog-floor를 git 저장소로 (`cec56ad`)

**왜**: 수익 나는 앱인데 버전관리 밖에 있어서 잘못 고치면 되돌릴 수 없었습니다.

**담은 것**: 소스 34개.
**뺀 것**: `data/` 전부, `node_modules`.

> ⚠️ `data/settings.json`에 **Anthropic API 키와 네이버 광고 API 키**가 평문으로 들어 있습니다.
> `.gitignore`가 막고 있고, 커밋 전에 전 파일 키 패턴 검색으로 유출이 없음을 확인했습니다.
> **원격 저장소에 올릴 일이 생기면 이 파일이 절대 안 올라가는지 다시 확인하세요.**

---

## 6. 기록된 정책 (메모리)

| 파일 | 내용 |
|---|---|
| `policy_max20x_first.md` | MAX 20X 우선. **추가**: "PC 켜져있을 때만 작업" — 24/7 클라우드 크론은 새로 만들지 않음. 모든 앱·홈페이지 공통 |
| `feedback_saebyeok_beauty_overrides.md` | 새벽뷰티 계열 스킬에 덧붙일 화장품·헤어 4규칙 |

---

## 7. 남은 일

### 막힌 것

**옵시디언 vault 연결** — 이 PC에 **옵시디언이 설치돼 있지 않습니다.**
설정 폴더(`AppData/Roaming/obsidian`)도 vault 등록 파일(`obsidian.json`)도 없습니다.
스킬 파일만 깔아두고 `OBSIDIAN_VAULT_PATH`는 비워뒀습니다.
없는 경로를 적어두면 스킬이 조용히 엉뚱한 데 쓰거나 매번 실패하기 때문입니다.

→ 옵시디언 설치 + vault 폴더 생성 후 경로를 주면 `~/.claude/settings.json`에 연결(1분).
→ 리서치 툴킷(`integrations/` MCP 서버)은 지시대로 건너뜀. API 키·uv 필요.

### 다른 곳에서 처리 중

**통합복사** — 다른 세션에서 처리하기로 하여 이 세션은 건드리지 않았습니다.

### 확인 필요

- **`woosurimi-trend-api`에 미커밋 37건.** 이번에 제가 고친 `src/homefeedRules.js`도 섞여 있고,
  나머지 대부분은 이전부터 쌓여 있던 것(확장·썸네일·영상 등)입니다. 커밋 범위는 사장님 판단이 필요합니다.
- **`src/homefeedRules.js`는 다른 세션도 편집합니다.** 같은 파일을 양쪽에서 고치면 충돌합니다.

---

## 8. 복원·재현 방법

```bash
# 앱 실행 (PC 켜져 있는 동안만 돎)
node golden-blog-floor/server.js          # http://localhost:8485

# 홈판 분석 다시 재기 (AI 0원, 약 3분)
cd woosurimi-trend-api
node scripts/homefeed-deep-v2.js          # 수집 → data/homefeed-deep-v2.json
node scripts/homefeed-deep-report2.js     # 집계 출력
```

- 이번 코드 변경을 되돌리려면: `golden-blog-floor`에서 `git log`로 `cec56ad` 확인 후 `git diff`/`git revert`.
- 스킬 지침 숫자를 바꾸려면 `woosurimi-trend-api/src/homefeedRules.js`의 `MATE` 한 곳만 고치면
  모든 스킬에 함께 반영됩니다.

---

## 9. 이 세션에서 새로 만든 파일

| 파일 | 용도 |
|---|---|
| `woosurimi-trend-api/scripts/homefeed-deep-analyze.js` | 1차 수집(도입부·Q&A 측정이 깨진 버전) |
| `woosurimi-trend-api/scripts/homefeed-deep-v2.js` | **2차 수집(정본)** — 문단 단위로 정확히 측정 |
| `woosurimi-trend-api/scripts/homefeed-deep-report.js` | 1차 집계 |
| `woosurimi-trend-api/scripts/homefeed-deep-report2.js` | **2차 집계(정본)** |
| `바탕화면/홈판-상위노출-해부.pdf` | 리포트 PDF 11쪽 |
| `~/.claude/commands/caveman.md` | `/caveman` 슬래시 명령 |
| `~/.claude/superpowers/hooks/session-start-cc` | superpowers 훅 래퍼 |

> 1차 스크립트를 지우지 않고 남긴 이유: `bodyText`에 줄바꿈이 없어 도입부·Q&A 측정이 깨졌던
> 경위가 파일에 주석으로 남아 있습니다. 다음에 같은 함정을 다시 밟지 않으려면 필요합니다.
